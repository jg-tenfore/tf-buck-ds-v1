import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Introduction" />

<p style={{ fontSize: 13, fontWeight: 700, letterSpacing: "0.14em", textTransform: "uppercase", color: "#667085", margin: "0 0 6px" }}>
  Tenfore
</p>

# Buck Design System

<p style={{ fontSize: 20, lineHeight: 1.55, color: "#475467", maxWidth: 680, margin: "6px 0 0" }}>
  Tenfore's component library and design foundations — built on Tailwind CSS v4 and
  React Aria, and documented end-to-end here in Storybook.
</p>

<p style={{ fontSize: 14, color: "#98a2b3", margin: "18px 0 0" }}>
  Foundations &nbsp;·&nbsp; Components &nbsp;·&nbsp; Sign in / Sign up &nbsp;·&nbsp; WCAG AA
</p>

## ▶ Interactive prototype

<a href="http://localhost:6019" target="_blank" rel="noreferrer" style={{ textDecoration: "none" }}>
  <div style={{ border: "1px solid #eaecf0", borderRadius: 16, padding: "22px 24px", margin: "20px 0 4px", background: "linear-gradient(180deg,#fcfcfd,#f9fafb)" }}>
    <p style={{ fontSize: 12, fontWeight: 700, letterSpacing: "0.14em", textTransform: "uppercase", color: "#16803c", margin: 0 }}>
      Standalone app · Runs on its own
    </p>
    <p style={{ fontSize: 18, fontWeight: 650, color: "#101828", margin: "8px 0 4px" }}>
      Open the Tenfore App prototype&nbsp;→
    </p>
    <p style={{ fontSize: 14, lineHeight: 1.55, color: "#475467", margin: 0, maxWidth: 640 }}>
      Every App Screen stitched together behind the real Global Nav. Click any sidebar item — or press
      <kbd style={{ margin: "0 4px", padding: "1px 6px", border: "1px solid #d0d5dd", borderRadius: 6, fontSize: 12 }}>⌘K</kbd>
      — and the screen swaps in place, just like the live product. It runs <strong>outside Storybook</strong> as a
      dedicated app, so it stays fast and self-contained.
    </p>
    <p style={{ fontSize: 13, fontWeight: 600, color: "#16803c", margin: "12px 0 0" }}>
      localhost:6019 &nbsp;·&nbsp; opens in a new tab
    </p>
  </div>
</a>

The interactive prototype is a working, self-contained walkthrough of the back-office. It renders the
dual-tier Global Nav once and drives navigation entirely client-side. It reuses the *exact* same screen
components documented here under **App Screens**, so the prototype and the component library never drift
apart. Any nav destination that isn't designed yet shows a clear "not built" placeholder instead of dead-ending.

**How to run it** — it lives in its own local, separate from this Storybook:

- Run <code>npm run app</code>, then open <a href="http://localhost:6019" target="_blank" rel="noreferrer"><strong>http://localhost:6019</strong></a> (Storybook stays on 6018).
- Hover a top-level section to reveal its sub-items, then click to navigate.
- Press <kbd>⌘K</kbd> / <kbd>Ctrl-K</kbd> anywhere to jump to any screen by name.

## What this is

This Storybook is the single source of truth for the **Buck Design System** — the tokens, primitives,
and component library that new Tenfore product screens are built from. It shares its color palette and
typography with the Fox design system, on the same Tailwind v4 + React Aria foundation.

## The map

| Category | What's inside |
| --- | --- |
| **Foundations** | Design tokens & primitives — Colors, Typography, Spacing, Radius, Border, Effect Styles, Icons, and Logos. |
| **Components** | The full kit — Actions, Forms, Feedback & Status, Layout & Structure, Media & Visuals, and Navigation. |
| **Sign in / Sign up** | Authentication flows — Log in, Sign up, Forgot password, Verification. |

## How it's built

- **React 19** + **TypeScript** throughout
- **Tailwind CSS v4** with a semantic, light/dark-aware token system (`src/styles/theme.css`, `palette.css`)
- **React Aria Components** as the accessibility foundation (imported as `Aria*`)
- **Storybook 10** (`@storybook/nextjs-vite`) for documentation and visual testing

### Conventions

- Files are **kebab-case**; React Aria imports are prefixed **`Aria*`**.
- Style with **semantic tokens** (`text-secondary`, `bg-primary`) — never literal color classes.

---

<p style={{ fontSize: 13, color: "#667085" }}>
  Tip: use the sidebar to browse, or press <kbd>/</kbd> to search. Most stories have a <strong>Controls</strong> tab
  to tweak props live, and a <strong>Docs</strong> tab for usage.
</p>
